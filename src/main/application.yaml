#spring:
#  application:
#    name: JavaAndApiDevelopment

spring:
  application:
    name: JavaAndApiDevelopment
  jpa:
    show-sql: true
h2:
    console:
      enabled: true
datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: sa 
    password:  
jpa:
   hibernate:
     ddl-auto: create
     database-platform: org.hibernate.dialect.H2Dialect
